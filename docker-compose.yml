version: '3'

services:
  bluetooth:
    image: amjadbadar05/mx-ble-mqtt:0.0.4-armhf
    network_mode: host
    privileged: true
    cpus: 0.1
    mem_limit: 20m
    restart: always
    logging:
      driver: json-file
    command: --ipv4="127.0.0.1"
